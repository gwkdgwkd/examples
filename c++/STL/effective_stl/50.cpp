#include <iostream>

using namespace std;

// 熟悉与STL相关的Web站点

// 一位STL程序员都会经常光顾的站点：
//  SGI STL站点：http://www.sgi.com/tech/stl/
//  STLport站点:http://www.stlport.org
//  Boost站点:http://www.boost.org

// SGI STL位居榜首，所有组件都提供了详尽的文档。无论使用哪个STL平台，这个站点都是在线参考手册。
// 提供了一个可以免费下载的STL实现，仅仅被移植到了少数几个编译器上，但却是被广泛使用的STLport的基础。
// 提供了许多非标准组件，使得程序设计更加强大、灵活和有趣。
//  哈希关联容器hash_set、hash_multiset、hash_map和hash_multimap。
//  单向链表容器slist。
//  针对大文本的与string类似的容器，别称为rope。
//  各种非标准的函数对象和配接器。

// STLport最大的卖点在于，提供了一个可以在超过20中编译器上使用的SGI STL改进版本。
// 与SGI STL一样，STLport也可以免费下载。
// STLport对SGI STL的改进绝大多数是处于移植性的考虑。还提供了一种“调试模式”，可以检测到各种常见的错误。

// Boost Web站点的宗旨是“提供免费的、公开审视的C++库。重点在那些可以与C++标准库很好协同工作的可移植性库上”。
// Boost站点提供了大量的C++库。
// Boost中第二个吸引STL爱好者的地方是一些与STL相关的函数对象，以及相关的设施。

int main() { return 0; }
